<p style="color: green"><%= notice %></p>

<h2 class="pb-3 titulo">Inicio</h2>
<hr>

<div id="tweets">
  <% @tweets.each do |tweet| %>
    <%= render tweet %>
  <% end %>
  <div class="btn btn-primary">
  <%== pagy_nav(@pagy) if @pagy.pages >1  %>
  </div>
</div>
